/// <reference path path="utility-functions.ts" />

const result1 = Utility.maxBooksAllowed(30);
console.log(result1);

import util = Utility.Fees;
const result2 = util.calculateLateFee(3);
console.log(result2);
